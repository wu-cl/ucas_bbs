<div class="panel panel-default col-md-10 col-md-offset-1">
  
  <div class="panel-body">
    
    <div class="group">
      <div class="row">
        <div class="col-sm-2 col-lg-1 ">
        </div>
        <%= render :partial => "homeland/searcht/search_bar" %>
      </div>
    </div> 

    <br>
    <% if @query_string.blank? %>
        <h4 class="search-info-title">搜索信息不能为空，请输入关键字搜索</h4>
    <% elsif @searcht.blank? %>
        <br>
        <h4 class="search-info-title">亲，没有搜索到相关工作信息哦，要不换个关键词试试？</h4>
    <% else %>
        <h4 class="search-info-title"> 有关"<%= @query_string %>"的工作信息 </h4>
        <hr>

        <div class="search-result">

          <% @searcht.each do |st| %>
           <div class="st-item">
             <p><span><%= user_avatar_tag(st) %><span class="pull-right">更新于 <%= timeago st.activity_at %></span></p>
             <h4><%= link_to st.title, homeland.topic_path(st) %></h4>
             <p><i class="fa fa-street-view" aria-hidden="true"></i> <%= node_tag(st.node) %></p>
             <p><i class="fa fa-clock-o" aria-hidden="true"></i> <%= st.replies_count %></p>
             <p><span class="label label-success"><%= st.replies_count %></span></p>
             <hr>
           </div>
          <% end %>
        </div>
        <% if @searcht.present? %>
        <div class="text-center">
          <%= will_paginate(@searcht, renderer: BootstrapPagination::Rails) %>
        </div>
        <% end %>
    <% end %>
  </div>
</div>