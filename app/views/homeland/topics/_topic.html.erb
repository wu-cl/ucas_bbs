<!--
<tr class="topic">
  <td class="title"><%= link_to topic.title, homeland.topic_path(topic) %></td>
  <td class="node"><%= node_tag(topic.node) %></td>
  <td class="author"><%= user_avatar_tag(topic) %></td>
  <td class="replies"><%= topic.replies_count %></td>
  <td class="activity"><%= timeago topic.activity_at %></td>
</tr>
-->

<li class="article-entry standard">
  <h4><%= link_to topic.title, homeland.topic_path(topic) %></h4>
  <span class="article-meta">
    WCL in
    <a href="#" title="View all posts in"
      ><%= node_tag(topic.node) %></a
    ></span
  >
  <span class="like-count"><%= topic.replies_count %></span>
</li>

